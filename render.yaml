services:
  - type: web
    name: fastapi-app
    env: python
    plan: free
    runtime: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    region: oregon
    branch: main
